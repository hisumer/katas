export function getFizzbuzz(number) {
    let message = []
    for (let index = 1; index <= number; index++) {
        switch (true) {
            default:
                message.push(index)
        }
    }

    return message;
}
